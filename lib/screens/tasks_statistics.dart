import 'package:flutter/material.dart';
import 'package:fl_chart/fl_chart.dart'


class TasksStatisticsScreen extends StatelessWidget {
  const TasksStatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: SizedBox(

        height: 200,

        child: Text('Chart'),

      ),
    );
  }
}
